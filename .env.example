# Copy to .env before running locally
# cp .env.example .env

# Local or remote RPC endpoint for Ethereum
RPC_URL=http://localhost:8545

# Network name (e.g., mainnet, Rinkeby, Sepolia)
NETWORK=development

# Optional API key for external services
API_KEY=

# Log level for debugging (info | debug | warn)
LOG_LEVEL=info
